# H课程表 · 打包与发布指南

> 面向：把 App 打包成安卓/苹果安装包，分享给别人用；以及日常运行、测试、GitHub 发布。

---

## 1. 环境准备

- 电脑：Node 24+ / Git
- 手机：安卓（Expo Go 或装 APK）、iPhone/iPad（Expo Go 或 TestFlight）
- 网络：**本地预览关 VPN；打包/推送 GitHub 开 VPN**

---

## 2. 本地运行（开发/预览）

### 浏览器预览（最快，无需手机）
```bash
cd "E:\bbb\Univercity schoolwork\syllabus\h-course-table"
npx expo start --web
```
浏览器打开 http://localhost:8081

### Expo Go 真机预览（边开发边看）
```bash
npm start
```
- 安卓手机：安装 Expo Go SDK57 的 APK，扫码或输入终端里的 exp://IP:8081
- iPhone/iPad：App Store 的 Expo Go（版本需支持 SDK57），同样输入 exp://IP:8081
- 跨网络：npm start -- --tunnel（需外网）

> 手机和电脑要在同一网络（同一 Wi-Fi 或手机热点，且电脑连该热点）。

### 测试
```bash
npm test           # 34 个单测
npx tsc --noEmit   # 类型检查
```

---

## 3. 安卓打包 APK（推荐先做这个）

1. 安装 EAS CLI（一次性）
```bash
npm install -g eas-cli
```
2. 登录 Expo 账号（浏览器弹窗）
```bash
eas login
```
3. 在项目目录执行（首次会提示创建 EAS 项目，回车确认即可）
```bash
cd "E:\bbb\Univercity schoolwork\syllabus\h-course-table"
npx eas-cli build --platform android --profile preview
```
4. 构建完成后，终端/邮件里会给一个下载链接（Android 用 preview 配置会打 APK）
5. 手机浏览器打开链接 → 下载 .apk → 安装（允许“安装未知来源”）

> 项目已带 eas.json，preview=APK、production=App Bundle；图标已是抹茶绿 H，打包后桌面新图标生效。

### 常见问题
- 提示需要登录 → 执行 eas login
- 构建时间 5–15 分钟，需要网络（建议 VPN）
- 报权限/证书问题 → 用 `npx eas-cli build:run` 看详细日志；或发我

---

## 4. 苹果 iOS（TestFlight / App Store）

苹果不能像安卓一样直接发 APK，需 Apple 开发者账号（99 美元/年）。

### 方式一：TestFlight（小范围测试，推荐）
```bash
cd "E:\bbb\Univercity schoolwork\syllabus\h-course-table"
npx eas-cli build --platform ios --profile preview
npx eas-cli submit --platform ios   # 上传到 TestFlight（首次需在苹果后台建 App 记录）
```
然后在 App Store Connect 添加测试员，对方装 TestFlight 后输入邀请码即可。

### 方式二：App Store 上架（正式，需审核）
- 在 App Store Connect 创建 App、填元数据/截图/隐私、提交审核（1–7 天）
- 用 `npx eas-cli build --platform ios --profile production` 出包后 submit

---

## 5. GitHub 发布（已基本完成）

- 仓库：https://github.com/qqing9271-hub/h-course-table
- main 分支已推送；v1.0.0 标签已推送
- 发布 APK：在 GitHub Releases 页 → Create release → 选 v1.0.0 → 上传 APK → Publish

---

## 6. 常见问题速查

| 问题 | 原因/解决 |
|---|---|
| 手机连不上 | 电脑 npm start 没开；或 VPN 干扰（本地预览关 VPN） |
| Expo Go 不兼容 | 安卓装 SDK57 的 APK；iPad 用 App Store 最新版或浏览器预览 |
| 导入 Excel 失败 | 已在 Android 修复（copyToCacheDirectory:false + copyAsync）；仍失败发我报错 |
| 图标没变 | 需重装 APK（重建后生效）；Web 预览用浏览器缓存刷新 |
| 推送 GitHub 失败 | 开 VPN；或你自己终端 git push origin main |

---

## 7. 项目结构（简）

```text
App.tsx                 # 入口 + 底部导航
src/screens/            # 首页/计划/随笔/课表/设置
src/components/         # 课程编辑/日历/等
src/domain/             # 学期/课表/计划/备份/分享 纯逻辑
src/store/              # zustand 状态 + 本地持久化
__tests__/              # 34 个单测
eas.json / app.json / LICENSE / README / CHANGELOG / .github/workflows/ci.yml
```