# H课程表 · 国内 Web 部署 + 苹果用户使用教程

## 一、为什么用 Gitee 而不是 GitHub/Vercel

- Gitee Pages：国内直连、免费、稳定；适合 iPhone/iPad/Safari「添加到主屏幕」当 App 用。
- GitHub Pages / Vercel：国内多数情况访问慢或被墙，不推荐作为国内主力。

---

## 二、部署到 Gitee（一次操作）

### 1. 你有 Gitee 账号吗
- 没有：注册 https://gitee.com （需实名认证，免费）
- 有：直接下一步

### 2. 新建公开仓库
- 右上角 + → 新建仓库
- 仓库名：`h-course-table-web`；**选公开**；不要勾选任何初始化文件
- 复制仓库地址，形如：
```text
https://gitee.com/你的用户名/h-course-table-web.git
```

### 3. 把 Web 构建产物推上去（在电脑终端执行）
```powershell
cd "E:\bbb\Univercity schoolwork\syllabus\h-course-table"
git init
git add dist
git commit -m "deploy web"
git branch -M main
git remote add origin https://gitee.com/你的用户名/h-course-table-web.git
git push -u origin main
```
（推送时若弹 Gitee 登录，用你的 Gitee 账号登录一次即可；不要发我密码）

### 4. 开启 Gitee Pages
- 打开仓库页 → **服务** → **Gitee Pages**
- 选择部署分支：`main` → 点「**启动**」
- 得到网址：`https://你的用户名.gitee.io/h-course-table-web/`

---

## 三、苹果用户怎么用（添加到主屏幕 = 像 App）

1. 用 **iPhone/iPad 的 Safari** 打开上面网址；
2. 点底部 **分享按钮**（方框+向上箭头）；
3. 选择 **「添加到主屏幕」**；
4. 可改名字（H课程表）→ 右上角 **添加**；
5. 桌面出现图标，点开即**全屏像 App 一样使用**。

> 说明：浏览器版 = 和手机 App 基本相同的界面（课表/计划/随笔/设置/多学期/深色/字号都可用）；仅**相机扫码/Excel 文件导入**这类原生功能受限（Web 上 Excel 可用浏览器选择文件导入，扫码不可用）。

---

## 四、后续更新（Web）

- 重新 `npx expo export --platform web` 生成新的 dist；
- 再 `git add dist && git commit && git push`；
- Gitee Pages 里点「**更新**」即可（有的需手动更新，有的自动）。